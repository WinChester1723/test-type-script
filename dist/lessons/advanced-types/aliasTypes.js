"use strict";
function fetchTestInAlias(url, method) {
    if (method === "post") {
        return 1;
    }
    else {
        return -1;
    }
}
let user = {
    user: {
        name: "Johnny",
        age: 35,
        skills: ["1", "2"],
    },
    role: {
        name: "Admin",
        id: 1,
    },
};
